import _extends from "@babel/runtime/helpers/extends";
import React, { forwardRef } from "react";
import Box from "../Box";
var Absolute = forwardRef(function (props, ref) {
  return React.createElement(Box, _extends({
    position: "absolute",
    ref: ref
  }, props));
});
export default Absolute;