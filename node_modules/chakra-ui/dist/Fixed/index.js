import _extends from "@babel/runtime/helpers/extends";
import React, { forwardRef } from "react";
import Box from "../Box";
var Fixed = forwardRef(function (props, ref) {
  return React.createElement(Box, _extends({
    position: "fixed",
    ref: ref
  }, props));
});
export default Fixed;